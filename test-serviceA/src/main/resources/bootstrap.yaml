spring:
  application:
    name: test-serviceA
  cloud:
    config:
      name: config
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: cloud-config-server

server.port: 7003
eureka.client.serviceUrl.defaultZone: http://localhost:1111/eureka/


